import type { FooterContent } from '../types'

const footer = {
	introduction: 'Ieder goed gesprek start met een simpele \'hallo\'. Of het nou over design, side projects, of totaal iets anders is; mijn inbox is altijd open.'
} satisfies FooterContent

export default footer
