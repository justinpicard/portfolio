import type { FooterContent } from '../types'

const footer = {
	introduction: 'Every great conversation starts with a simple \'hello\'. Whether it’s about design, side projects, or something completely different, my inbox is always open.'
} satisfies FooterContent

export default footer
